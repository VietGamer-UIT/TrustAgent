# =============================================================================
# TrustAgent :: Python Dependencies
# =============================================================================

# --- Core web framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9
starlette>=1.0

# --- LangGraph / LangChain multi-agent ---
langgraph==0.2.34
langchain==0.3.2
langchain-core==0.3.9
langchain-community==0.3.1
langchain-huggingface==0.1.0
langchain-google-genai==2.0.1
google-generativeai==0.8.2
# --- Pydantic v2 (MCP server validation) ---
pydantic>=2.7.0
pydantic-settings>=2.2

# --- Async PostgreSQL - Audit Trail (COMMIT 3) ---
sqlalchemy[asyncio]>=2.0.28
asyncpg>=0.29.0             # PostgreSQL asyncio driver
alembic>=1.13.0             # DB migration management

# --- HTTP clients (live MCP calls) ---
aiohttp>=3.9.0
httpx>=0.27.0

# --- Typing backport ---
typing-extensions>=4.11.0

# --- Environment & config ---
python-dotenv>=1.0.1

# --- Dev / CI tooling (not needed at runtime) ---
ruff>=0.4.0
pytest>=8.1.0
pytest-asyncio>=0.23.0

# --- Phase 1: Z3 Symbolic Engine ---
z3-solver>=4.12.0

# --- Phase 3.5: Legal RAG (optional) ---
chromadb>=0.5.0
unstructured>=0.14.0
markdown>=3.6

# --- Phase 4: Database (SQLite cho dev, PostgreSQL cho production) ---
aiosqlite>=0.19           # SQLite async driver (dev/test)

# --- Testing ---
loguru==0.7.2
requests==2.31.0
beautifulsoup4==4.12.2
playwright==1.40.0
playwright-stealth==1.0.6
signxml==3.2.0
rich==13.7.0
